# 塗装屋 サンプルサイト

このリポジトリは GitHub Pages 向けのシンプルな塗装屋ランディングページのモックです。

ローカルプレビュー:

```bash
python3 -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

デプロイ (GitHub Pages):

- リポジトリを GitHub にプッシュ
- 設定 → Pages で `main` ブランチ / `root` を選択

画像は `assets/img/`、スタイルは `assets/css/styles.css` にあります。
